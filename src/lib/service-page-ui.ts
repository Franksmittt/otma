/** Shared `sizes` hints for editorial grids (ContentImage). */
export const sizesThird = "(max-width: 768px) 100vw, 33vw";

/** Lede quartet: half width on small screens, quarter on lg+ */
export const sizesLedeTile = "(max-width: 1024px) 50vw, 22rem";
